#!/bin/bash
# Double-click this file on macOS to build the Unreal project.
# It will open a Terminal window and run the setup automatically.
cd "$(dirname "$0")"
bash ./setup.sh
echo ""
echo "Press any key to close..."
read -n 1
